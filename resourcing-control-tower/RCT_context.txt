Resource Assignation System (RAS) - Application Context
=======================================================

Last updated: 2026-07-09


Purpose
-------
Resource Assignation System (RAS), formerly Resourcing Control Tower, is an
offline Windows desktop application for leadership review of resource
assignation, billing, assignment end dates, PM ownership, PP financial KPI
movement, and RFS forecast coverage.

The app is local-only:
- No backend.
- No authentication.
- No database.
- Excel files are the source of truth.


Technology
----------
Frontend:
- React with Vite.
- MUI DataGrid.
- Recharts.

Desktop:
- Electron.
- Electron main process handles local filesystem access, Excel loading, file
  selection, and Excel export.
- Electron preload exposes safe IPC APIs with contextBridge.

Excel:
- SheetJS/xlsx.
- Supports `.xlsx`, `.xls`, `.xlsm`, and `.xlsb` where enabled by module.

Packaging:
- electron-builder.
- Windows NSIS installer.
- Installer output:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


Build Commands
--------------
- `npm.cmd run build`
- `npm.cmd run dist`

Recently verified:
- Both commands completed successfully after the RAS naming and PP period
  comparison updates.

Packaging notes:
- `vite.config.js` uses `base: './'` so packaged Electron can load renderer
  assets from `file://`.
- Electron-builder currently uses the default Electron icon because no custom
  icon is configured.


Current Modular Layout
----------------------
The app now uses a modular shell.

Primary left navigation:
- RAS
- PP Report Analysis
- RFS Tracker

Layout files:
- `src/layout/MainLayout.jsx`
  - Main sidebar and module routing outlet.
- `src/layout/ResourcingLayout.jsx`
  - Resourcing-only top bar and outlet.

Top bar rules:
- The Excel top bar is visible only for RAS/Resourcing routes.
- PP Report Analysis and RFS Tracker use their own local page controls.


Routing
-------
RAS routes:
- `/resourcing/home`
- `/resourcing/leadership-actions`
- `/resourcing/pm-accountability`
- `/resourcing/resource-master`
- `/resourcing/billing-status`
- `/resourcing/end-date-tracker`
- `/resourcing/skill-capability`
- `/resourcing/bench`
- `/resourcing/alerts`
- `/resourcing/export`

Other module routes:
- `/pp-report/overview`
- `/pp-report/rule-config`
- `/rfs-tracker/overview`

Route compatibility:
- Older internal route keys such as `home`, `resources`, `billing`, and
  `endDates` are mapped to the new Resourcing route keys so existing KPI
  navigation still works.


Electron IPC Bridges
--------------------
Resource bridge:
- `window.resourceBridge.getResourceData()`
- `window.resourceBridge.selectExcelFile()`
- `window.resourceBridge.exportWeeklySnapshot(payload)`

PP bridge:
- `window.ppBridge.getPpData()`
- `window.ppBridge.selectPpExcelFile()`
- `window.ppBridge.exportPpAnalysis(payload)`

RFS bridge:
- `window.rfsBridge.getRfsData()`
- `window.rfsBridge.selectRfsExcelFile()`
- `window.rfsBridge.exportRfsSummary(payload)`

Comments bridge:
- `window.commentsBridge.getCommentsData()`
- `window.commentsBridge.saveResourceComment(payload)`
- `window.commentsBridge.savePpComment(payload)`


Settings Persistence
--------------------
Electron user-data `settings.json` stores selected workbook paths:
- `selectedWorkbookPath`
- `selectedPpWorkbookPath`
- `selectedRfsWorkbookPath`

This allows refresh to reload the same selected workbook after file selection.

Electron user-data `comments.json` stores editable comments:
- `resource`
  - RAS PM comments / PMO comments keyed by employee, customer, project, and
    assignment end date.
- `pp`
  - PP PM Remarks keyed by period type, current period, customer, and KPI.

Comment persistence rules:
- Comments are local to the Windows user profile.
- Source Excel workbooks are not modified.
- Reloading the app or refreshing Excel reapplies saved comments where keys
  match the current workbook rows.


Ignored Local Data
------------------
`.gitignore` excludes:
- `node_modules/`
- `dist/`
- `release/`
- local Excel files under `data/*.xlsx`, `data/*.xls`, `data/*.xlsm`
- `data/exports/*` except `.gitkeep`


RAS MODULE
==========

Note:
- Internal route/module keys still use `resourcing` for compatibility.
- Visible application naming is now Resource Assignation System (RAS).
- The left navigation parent is `RAS`.

Default Input
-------------
Default file:
- `data/Resource_Data.xlsx`

User-selected file:
- Chosen through the Resourcing top bar.
- Supports `.xlsx`, `.xls`, and `.xlsm`.
- Refresh reloads the selected workbook.

Parsing:
- Electron loads the workbook with SheetJS.
- First worksheet is parsed.
- Rows are normalized in `src/services/dataDerivation.js`.


RAS Top Bar
-----------
Visible only under RAS/Resourcing routes.

Controls:
- Loaded Excel file name.
- Loaded timestamp.
- Employee Name / Employee Code search.
- Customer quick filter.
- Project quick filter filtered by selected customer.
- PM Name multi-select.
- Location filter.
- Choose Excel.
- Refresh Excel.


Normalized Fields
-----------------
Key normalized fields:
- `empId`
- `employeeName`
- `band`
- `subBand`
- `location`
- `manager`
- `capability`
  - Mapped from `HR L4`.
- `skills`
  - From `Employee Skill Name`.
- `fresherClassification`
  - From `Fresher/FCL /Lateral`.
- `projectName`
- `projectCode`
- `wbsCode`
- `wbsCodeCategory`
- `customer`
- `pmName`
  - From `PM_Name`.
- `pmoComments`
  - From `PMO_COMMENTS`.
- `billingStatus`
- `allocationPct`
- `fte`
- `assignmentStart`
- `assignmentEnd`


Billing / WBS Rules
-------------------
Billing Status:
- `B` = `Billable`
- `C` = `Contractual Shadow`
- `N` = `Non Billable`

Y-Code / C-Code:
- Derived from the first character of `WBS Code`.
- Starts with `Y` => `Y-Code`.
- Starts with `C` => `C-Code`.
- Otherwise => `Other`.


FTE Counting Rule
-----------------
All leadership counts use FTE-weighted values, not unique employee count.

Example:
- If a resource has FTE `0.25`, it contributes `0.25`.

Fallback:
- If FTE is missing, FTE falls back to `allocationPct / 100`.

Allocation normalization:
- `1` becomes `100`.
- `0.25` becomes `25`.


Derived Resourcing Logic
------------------------
End-date:
- `daysToEnd` = assignment end date minus current date.
- `endRiskFlag` = true when `daysToEnd` is between 0 and 30.

Bench:
- True when Billing Status is `Non Billable`, or WBS type is `Y-Code` or
  `C-Code`.

Fresher:
- True when `Fresher/FCL /Lateral` starts with `Fresher`.

Utilization:
- `(Total FTE - Non Billable FTE) / Total FTE * 100`


RAS Pages
---------
Home / Executive View:
- Total FTE.
- Utilization %.
- Billable Count.
- Contractual Shadow.
- Non Billable Count.
- Y-Code Count.
- C-Code Count.
- Freshers.
- Ending <=30 Days.
- Billing Status donut.
- Customer horizontal bar chart.
- Band pyramid.
- Offshore/Onsite summary.

Leadership Action Queue:
- Prioritized action list.
- Action types:
  - End Date Decision.
  - End Date Follow-up.
  - Non Billable Conversion.
  - Y-Code Review.
  - C-Code Review.
- `Missing PMO Comment` is intentionally excluded.
- PM comments are editable.
- Edited PM comments persist locally in Electron `userData/comments.json`.
- Edited comments are included in Weekly Snapshot Export.

PM Accountability View:
- PM-wise FTE, utilization, billing, end-date, and high-priority action metrics.

RAS Master:
- Full DataGrid with sorting/filtering.
- Search by employee name/code.
- Filters:
  - Customer.
  - Project dependent on customer.
  - PM Name multi-select.
  - Billing Status.
  - WBS Code Type.
  - Band.
  - Location.
- Row click opens detail modal.

Billing & Status Dashboard:
- Tabs:
  - All.
  - Billable.
  - Contractual Shadow.
  - Non Billable.
  - Y-Code.
  - C-Code.

Assignment End-Date Tracker:
- Filters:
  - Ending <=15 days.
  - Ending <=30 days.
  - Ending <=60 days.
  - Custom range.
- Color coding:
  - Red <=15.
  - Orange 16-30.
  - Yellow 31-60.
- PMO_COMMENTS is shown next to Employee Name.

Skill & Capability View:
- HR L4 filter.
- Skill heat map from `Employee Skill Name`.
- Resource table for selected scope.

Bench / Y-Code / C-Code:
- Tabs:
  - All.
  - Bench.
  - Y-Code.
  - C-Code.
  - Non Billable.

Alerts & Actions:
- Assignment end risk.
- Non Billable ageing >30 days.

Export & Reports:
- Weekly Snapshot Export.
- Exports:
  - `Executive_KPIs`
  - `Leadership_Actions`
  - `PM_Accountability`
  - `Resource_Detail`
- Includes edited PM comments from current session.


PP REPORT ANALYSIS MODULE
=========================

Default Input
-------------
Default workbook:
- `C:\Users\nooka.nageswararaok\Downloads\Final_MEGA Financial Trend FY'26 @FX27 Rates_IND.xlsb`

Reference workbook used for target layout:
- `C:\Users\nooka.nageswararaok\OneDrive - HCL TECHNOLOGIES LIMITED\Documents\Nagesh\Engagements\PP Report\ERS MEGA - GM Trend @FX27 rate YTD Apr'26_MFG.xlsb`

Reference worksheet:
- `KPI Comparison`

Instruction source:
- `C:\Users\nooka.nageswararaok\Downloads\CONSOLIDATED PROMPT FOR KPI COMP.md`

Parsing:
- Electron loads the `Base` sheet using SheetJS dense mode.
- `.xlsb`, `.xlsx`, `.xls`, and `.xlsm` are supported through Choose PP Excel.
- Dense mode was required because full `.xlsb` parsing is large.
- Oversized `.xlsx` PP reports whose `Base` worksheet XML exceeds Node's
  single-string limit are handled by a targeted streaming XLSX parser fallback.
  This was added for:
  `C:\Users\nooka.nageswararaok\OneDrive - HCL TECHNOLOGIES LIMITED\Documents\Nagesh\Engagements\PP Report\ERS MEGA - GM Trend YTD Jun'26_MFG.xlsx`.


PP Normalized Fields
--------------------
Rows are normalized in Electron main process before sending to React.

Key fields:
- `l4Mapping`
- `l3Mapping`
- `customerCode`
- `customerName`
- `customerGrouping`
- `componentGroup`
- `revisedComponentGroup`
- `projectCode`
- `projectName`
- `projectType`
- `majorProjectCategory`
- `costElement`
- `mode`
- `month`
- `quarter`
- `fiscal`
- `offshoreOnsite`
- `l2Pm`
- `l3Pm`
- `l4Pm`
- `totalRevenue`
- `revisedDrc`
- `revisedGm`
- `totalProjectExpenses`
  - Sourced from `Total Project Expenses` when present.
  - Falls back to `Project Expenses` for older PP workbooks.
- `badDepts`
  - Sourced from `Bad Depts` / `Bad depts`.
  - Also accepts `Bad Debts` / `Bad Debt` spelling variants.
- `totalGfte`
- `totalAfte`
- `totalBfte`


PP Filters
----------
Current filters:
- L4 Mapping Contains
  - Multi-select.
  - Defaults to all L4 mappings containing `DIGITAL ENGG1`.
  - Handles both INDUSTRIAL and MFG naming variants.
- Customer Groupings
  - Multi-select.
- Customer.
- Project.
- Period Type
  - Month.
  - Qtr.
  - Fiscal.
- Current Period
  - Label changes based on selected Period Type:
    - Current Month.
    - Current Qtr.
    - Current Fiscal.
- Comparison Periods
  - Multi-select.
  - Defaults to the previous three available periods for the selected Period
    Type where possible.
- Fiscal Filter
  - Disabled when Period Type is Fiscal to avoid duplicate filtering.

Period comparison behavior:
- Month mode compares selected month against selected comparison months.
- Qtr mode aggregates rows by `quarter` and compares selected Qtr against
  selected comparison Qtrs.
- Fiscal mode aggregates rows by `fiscal` and compares selected Fiscal against
  selected comparison Fiscal values.


PP KPI Order
------------
The detailed KPI comparison uses this order:
1. Total Revenue
2. Revised DRC
3. Revised GM
4. GM%
5. Total Project Expenses
6. Bad Depts
7. Realization
8. Total ARC
9. Total GFTE
10. Total AFTE
11. Total BFTE
12. Unbilled
13. GFTE Utili %
14. AFTE Utili %


PP KPI Formulas
---------------
Direct:
- Total Revenue
- Revised DRC
- Revised GM
- Total Project Expenses
  - Uses the source `Total Project Expenses` column when available.
  - Falls back to `Project Expenses` only for older PP workbooks that do not
    contain `Total Project Expenses`.
- Bad Depts
- Total GFTE
- Total AFTE
- Total BFTE

Calculated:
- Realization = Total Revenue / Total AFTE
- Total ARC = Revised DRC / Total GFTE
- GM% = Revised GM / Total Revenue
- GFTE Utili % = Total BFTE / Total GFTE
- AFTE Utili % = Total BFTE / Total AFTE
- Unbilled = Total AFTE - Total BFTE

Division by zero returns 0.


PP Output Layout
----------------
The PP screen is now report-focused and does not show KPI cards or charts.

Displayed sections:
- Filter panel.
- Detailed KPI Comparison by Customer.
- Grand Total Summary.

Removed sections:
- Customer-Wise Revenue Comparison.
- Revenue Summary by Customer.
- KPI Action Flags card.
- Grand Total Current vs 3-Month Average card.
- KPI cards.
- Charts.


Detailed KPI Comparison by Customer
-----------------------------------
Behavior:
- Customer results are grouped under collapsible cards.
- Each card title is the customer/customer group name.
- First customer card is expanded by default.
- Customer column is not shown inside the KPI grid.
- Grid rows are compact.

Columns:
- KPI.
- Each selected comparison period.
- 3-Month Avg in Month mode.
- Comparison Avg in Qtr/Fiscal modes.
- Current period.
- Change.
- RFS Value.
- Trend.
- Action Flag.
- PM Remarks.

PM Remarks:
- Editable in the grid.
- Persist locally in Electron `userData/comments.json`.
- Reloading PP Analysis reapplies saved remarks when period type, current
  period, customer, and KPI match.
- Not written back to source Excel.

Action Flag:
- Derived through a configurable PP rule engine.
- Rules are evaluated in the UI against the current filtered PP scope and, for
  Total Revenue, the matching RFS customer-group value where available.
- Action flag colors are configurable.

Current action rule behavior:
- Total Revenue:
  - Compare current period value against corresponding RFS value.
  - If PP revenue is lower: `Missing Revenue or UBR or RT based on RFS`.
  - Double-click opens Total Revenue details by `Cost Element_6`.
- Revised DRC:
  - Indicative DRC = previous period Total ARC * current period Total GFTE.
  - Above indicative DRC flags possible added high-cost resources.
  - Below indicative DRC flags possible released high-cost resources.
- Revised GM and GM%:
  - Positive movement flags improved GM.
  - Negative movement flags decreased GM.
- Total Project Expenses:
  - Double-click opens expense details by `Cost Element_6`.
  - Deviation above configured threshold, default 10%, is highlighted.
- Total GFTE, Total AFTE, Total BFTE:
  - Positive movement flags ramp up.
  - Negative movement flags ramp down.
- Realization:
  - Default improvement threshold is current value more than 10% above
    comparison average.
  - Decline flags missed revenue or unbilled resources.
- Total ARC:
  - Positive movement flags increased cost.
  - Negative movement flags decreased cost.
- GFTE Utili % and AFTE Utili %:
  - Default red below 80.
  - Default amber from 80 to below 90.
  - Default green at 90 and above.
- Unbilled:
  - Default threshold is >=10% of Total GFTE.


PP Color Coding
---------------
Applies to period/current/average/change numeric cells:
- Negative numbers are red by default.
- Negative number color is configurable in PP Rule Configuration.

GM% thresholds:
- GM% <=25: red by default.
- GM% 26-35: amber by default.
- GM% >=36: green by default.
- Thresholds are configurable.

Note:
- Negative number red takes priority over threshold coloring.


PP Rule Configuration
---------------------
Route:
- `/pp-report/rule-config`

Purpose:
- Allows business users to configure PP KPI action rules without code changes.

Persistence:
- Stored locally in browser storage inside the Electron renderer.
- No backend.
- No Excel source modification.

Configurable options:
- Enable/disable each KPI rule.
- Action flag messages.
- Thresholds:
  - GM%.
  - Utilization %.
  - Unbilled % of Total GFTE.
  - Expense deviation %.
  - Realization improvement %.
- Action flag and numeric cell colors:
  - Red.
  - Amber.
  - Green.
  - None.
- Revenue and expense drill-down toggles.

Configured KPI sections:
- Total Revenue.
- Revised DRC.
- Revised GM.
- GM%.
- Total Project Expenses.
- Bad Depts is shown as a report KPI but does not have a configurable action
  rule.
- Total GFTE.
- Total AFTE.
- Total BFTE.
- Realization.
- Total ARC.
- GFTE Utili %.
- AFTE Utili %.
- Unbilled.


PP Export
---------
Control:
- `Export PP` button on PP Report Analysis overview.

Output:
- Saves `.xlsx` through an Electron save dialog.

Sheets:
- `Detailed KPI Comparison`
  - Includes PM Remarks.
  - Includes Action Flag.
  - Includes RFS Value.
  - Uses selected comparison period columns.
- `Grand Total Summary`.
- `Filters and Source`.

PM Remarks:
- Editable in the grid.
- Persist locally in Electron `userData/comments.json`.
- Preserved in the exported PP workbook.
- Not written back to source Excel.


Grand Total Summary
-------------------
Behavior:
- Summarizes the current filtered PP scope.
- The Customer value is no longer hardcoded to `ALL CUSTOMERS`.
- It displays:
  - Selected customer name when the Customer filter is used.
  - Selected customer groups when Customer Groupings are selected.
  - `Selected Customers` otherwise.


RFS TRACKER MODULE
==================

Default Input
-------------
Default workbook:
- `C:\Users\nooka.nageswararaok\OneDrive - HCL TECHNOLOGIES LIMITED\Documents\Nagesh\Engagements\PP Report\RFS Tracker - FY'27 (10).xlsb`

Source worksheet:
- Depends on selected RFS Quarter:
  - AMJ: `base data-AMJ26`
  - JAS: `base data-JAS26`
  - OND: `base data-OND26`
  - JFM: `base data-JFM27`

Target output worksheet:
- Depends on selected RFS Quarter:
  - AMJ: `RFS Summary AMJ26`
  - JAS: `RFS Summary JAS26`
  - OND: `RFS Summary OND26`
  - JFM: `RFS Summary JFM27`

Instruction worksheet:
- `RFS Summary Prompt`

The prompt worksheet is used for implementation guidance but is no longer shown
in the UI.


RFS Calculation Rules
---------------------
Filter:
- Project Status = `Active`

Grouping:
- Group by `Customer Group`.

Monthly RFS:
- Selected quarter month RFS = Month Firm + Month MP
- AMJ uses Apr/May/Jun.
- JAS uses Jul/Aug/Sep.
- OND uses Oct/Nov/Dec.
- JFM uses Jan/Feb/Mar.

Total:
- Total RFS = selected quarter month RFS totals.
- Grand Total sums all visible filtered customer groups.


RFS Parsed Fields
-----------------
Key fields:
- `l4Name`
- `projectStatus`
- `year`
  - From `Year`, `Fiscal Year`, `FY`, or `RFS Year` when present.
  - Falls back to the selected RFS quarter sheet year suffix.
- `customerName`
- `customerGroup`
- `projectCode`
- `projectName`
- `pm`
- `eeEnNn`
  - From `EE/EN/NN`.
- `onOff`
  - From `On/Off ` or `On/Off`.
- Dynamic selected-quarter month fields:
  - `{monthKey}Firm`
  - `{monthKey}Mp`
  - Examples:
    - AMJ: `aprFirm`, `aprMp`, `mayFirm`, `mayMp`, `junFirm`, `junMp`.
    - JAS: `julFirm`, `julMp`, `augFirm`, `augMp`, `sepFirm`, `sepMp`.
    - OND: `octFirm`, `octMp`, `novFirm`, `novMp`, `decFirm`, `decMp`.
    - JFM: `janFirm`, `janMp`, `febFirm`, `febMp`, `marFirm`, `marMp`.


RFS UI
------
Controls:
- RFS Quarter selector:
  - AMJ.
  - JAS.
  - OND.
  - JFM.
- Choose RFS Excel.
- Refresh RFS.
- Export Summary.

Filters:
- Year.
- Customer Group
  - Multi-select.
- Project Manager.
- EE/EN/NN.
- On/Off.

Filters affect:
- KPI cards.
- Selected-quarter RFS Summary table.
- Active Source Detail table.
- Export Summary output.

KPI cards:
- Customer Groups.
- Active Projects.
- Selected quarter month RFS cards.
- Total RFS.

Tables:
- Selected-quarter RFS Summary table.
  - Customer Group.
  - Selected quarter month RFS columns.
  - Total RFS.
  - Grand Total row.
- Active Source Detail.
  - Includes Year.
  - Includes EE/EN/NN.
  - Includes On/Off.

Removed:
- `RFS Summary Prompt` card.

Export:
- `Export RFS` saves the current filtered RFS summary and Active Source Detail
  as `.xlsx`.
- `Export Detail` under Active Source Detail saves the current filtered source
  detail table as `.xlsx`.
- Exported workbook sheets:
  - Selected-quarter RFS summary sheet.
  - `Active Source Detail`.


RFS and PP Relationship
-----------------------
RFS Tracker is intended to support PP Analysis action flags.

Current status:
- RFS summary is calculated and filtered independently.
- PP Analysis now reads RFS summary values for Total Revenue action flag
  comparison when customer group names match.
- PP month comparison uses the matching RFS month field from the loaded
  quarter summaries.
- PP quarter comparison uses the matching RFS quarter total.
- PP fiscal comparison sums matching customer-group RFS values across AMJ,
  JAS, OND, and JFM.

Potential future refinement:
- Add deeper PP/RFS combined action models, such as:
  - Low GM% with strong RFS pipeline.
  - Revenue decline with weak RFS.
  - High unbilled with low future RFS coverage.


PROJECT EXPENSES MODULE
=======================

Current status:
- Removed from the active left navigation menu.
- Placeholder source file remains in the codebase for possible future reuse.

Page:
- `src/pages/expenses/ExpenseOverview.jsx`

Placeholder KPIs:
- Budget.
- Actual.
- Variance.

Message:
- Project expense analytics will be enabled using Excel uploads.


Known Limitations
-----------------
- Source Excel files are not modified.
- Editable RAS comments and PP remarks persist locally, but they are not yet
  synced across users or written back to source workbooks.
- PP and RFS are partially connected for Total Revenue RFS comparison but are
  not yet merged into a single combined action model.
- RFS supports AMJ, JAS, OND, and JFM source/output sheet selection.
- PP supports Month, Qtr, and Fiscal period comparison using workbook fields.
- No backend storage.
- No authentication or role-based access.
- No Power BI embedding yet.
- No desktop notification alerts yet.


Recommended Next Enhancements
-----------------------------
1. Add comment management controls.
   - Add clear/export/import options for locally persisted comments.
   - Show last-updated metadata for persisted comments.

2. Expand RFS-to-PP Action Flags.
   - Add RFS coverage indicators beyond Total Revenue.
   - Add combined PP/RFS action sheet.

3. Extend RFS-to-PP quarter awareness.
   - Use selected RFS quarter when comparing PP month/quarter values.
   - Add stronger validation when a selected PP period has no matching RFS
     quarter loaded.

4. Add PP/RFS export package.
   - Combine PP detailed KPI comparison with PM remarks.
   - Export RFS summary.
   - Include a combined action sheet.

5. Add app icon for packaged installer.


Version History
===============

v1.0.0 - Initial Resourcing MVP
-------------------------------
- Offline React/Electron app.
- Resource workbook loading from Excel.
- Home dashboard.
- Resource Master.
- Billing dashboard.
- Assignment end-date tracker.
- Skill and capability view.
- Bench/Y-Code/C-Code view.
- Alerts and export reports.


v1.1.0 - Excel Input and Resourcing Definitions
-----------------------------------------------
- Switched resource input from CSV to Excel.
- Added file picker for resource Excel.
- Persisted selected resource workbook path.
- Added billing status mappings:
  - B = Billable.
  - C = Contractual Shadow.
  - N = Non Billable.
- Added WBS-based Y-Code/C-Code derivation.
- Added Freshers KPI.
- Added HR L4 and skill heat map changes.
- Added PM_Name filters and PMO comments.


v1.2.0 - Leadership Actions and Weekly Export
---------------------------------------------
- Added Leadership Action Queue.
- Added PM Accountability View.
- Added Weekly Snapshot Export.
- Added FTE-weighted metrics.
- Excluded Missing PMO Comment action type.
- Added editable PM comments in Leadership Action Queue.


v1.3.0 - Modular Refactor
-------------------------
- Introduced module-level navigation:
  - Resourcing.
  - PP Report Analysis.
  - RFS Tracker.
  - Project Expenses.
- Moved Resourcing screens under `/resourcing`.
- Added `MainLayout`.
- Added `ResourcingLayout`.
- Resourcing top bar is now visible only under Resourcing routes.
- Added placeholder pages for PP, RFS, and Expenses.

Note:
- Project Expenses was later removed from the active left navigation in v2.0.0.


v1.4.0 - PP Analysis Implementation
-----------------------------------
- Added PP-specific Electron bridge.
- Added `.xlsb` PP workbook loading.
- Parsed PP `Base` sheet.
- Implemented KPI comparison formulas.
- Added current month vs comparison month calculations.
- Added reference-layout behavior from `KPI Comparison` worksheet.
- Added multi-select filters.
- Added default `DIGITAL ENGG1` L4 slice.
- Reworked PP UI to report-focused view.
- Added collapsible customer cards.
- Added Action Flag and editable PM Remarks.
- Added numeric color coding and GM% threshold colors.


v1.5.0 - RFS Tracker Implementation
-----------------------------------
- Added RFS-specific Electron bridge.
- Added `.xlsb` RFS workbook loading.
- Parsed `base data-AMJ26`.
- Implemented `RFS Summary AMJ26` calculation:
  - Active projects only.
  - Firm + MP by month.
  - Customer Group summary.
  - Grand Total.
- Added RFS filters:
  - Customer Group multi-select.
  - Project Manager.
- Added RFS export summary.
- Removed RFS prompt card from UI.
- Prepared RFS output for future PP Action Flag comparison.


v1.6.0 - PP Action Rule Engine and Export
-----------------------------------------
- Added configurable PP action rule behavior in the overview grid.
- Replaced Detailed KPI Comparison `Change %` column with `RFS Value`.
- Connected PP Total Revenue action flag to matching RFS customer-group value.
- Added Total Revenue drill-down by `Cost Element_6`.
- Added Total Project Expenses drill-down by `Cost Element_6`.
- Added configurable expense deviation threshold, default 10%.
- Added utilization color thresholds for GFTE Utili % and AFTE Utili %.
- Added `Export PP` Excel export.
- PP export preserves session PM Remarks.
- Added Electron IPC bridge:
  - `window.ppBridge.exportPpAnalysis(payload)`.


v1.7.0 - PP Rule Configuration Screen
-------------------------------------
- Added `PP Report Analysis > Rule Configuration`.
- Added route `/pp-report/rule-config`.
- Added configurable rules for all 13 PP KPIs.
- Added configurable action messages, thresholds, and color markings.
- Added local renderer persistence for PP rule configuration.
- Added reset-to-default and save behavior.


v1.8.0 - PP Period Comparison Modes
-----------------------------------
- Added Period Type selector to PP Report Analysis:
  - Month.
  - Qtr.
  - Fiscal.
- Added dynamic Current Period selector.
- Added dynamic Comparison Periods multi-select.
- Updated detailed KPI and grand-total calculations to aggregate by selected
  period type.
- Updated PP export metadata to include selected period type/current period and
  comparison periods.


v1.9.0 - RAS Naming Update
--------------------------
- Renamed visible application title from `Resourcing Control Tower` to
  `Resource Assignation System (RAS)`.
- Renamed left navigation parent from `Resourcing` to `RAS`.
- Renamed `Resource Master Dashboard` to `RAS Master`.
- Renamed Resource Master page heading to `RAS Master`.
- Updated packaged product name to `Resource Assignation System (RAS)`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.0.0 - RFS Quarter Selector and Navigation Cleanup
----------------------------------------------------
- Added RFS Quarter selector:
  - AMJ.
  - JAS.
  - OND.
  - JFM.
- RFS parser now automatically selects source worksheet and month columns:
  - AMJ -> `base data-AMJ26`, Apr/May/Jun.
  - JAS -> `base data-JAS26`, Jul/Aug/Sep.
  - OND -> `base data-OND26`, Oct/Nov/Dec.
  - JFM -> `base data-JFM27`, Jan/Feb/Mar.
- RFS summary output name now follows selected quarter.
- RFS KPI cards, summary grid, detail grid, and export columns are dynamic by
  selected quarter.
- Project Expenses was removed from the left navigation menu.


v2.1.0 - RFS Filters and Fiscal RFS Fix
---------------------------------------
- Added RFS filters:
  - EE/EN/NN.
  - On/Off.
- Added EE/EN/NN and On/Off columns to Active Source Detail.
- PP Analysis now loads all four RFS quarter summaries for action flag context.
- PP Fiscal comparison now sums RFS values across AMJ, JAS, OND, and JFM
  instead of reusing a single quarter total.


v2.2.0 - Local Comment Persistence
----------------------------------
- Added Electron `comments.json` under `userData`.
- Added comments IPC bridge:
  - `comments:getData`.
  - `comments:saveResource`.
  - `comments:savePp`.
- RAS Leadership Action Queue PM comments now persist locally.
- PP Detailed KPI Comparison PM Remarks now persist locally.
- Saved comments are reapplied on app reload and workbook refresh when their
  generated keys match current rows.
- Source Excel files remain unchanged.


v2.3.0 - Windows Application Icon Packaging
-------------------------------------------
- Added custom Windows icon assets under `build/`.
- Added runtime BrowserWindow icon.
- Added Electron App User Model ID for Windows taskbar grouping.
- Added local `afterPack` executable icon stamping using cached `rcedit-x64.exe`.
- Installer, unpacked executable, and installed shortcuts now use the same
  corporate application icon.


v2.4.0 - Resource Fulfilment Module
-----------------------------------
- Added new left navigation module: `Resource Fulfilment`.
- Added routes:
  - `/resource-fulfilment/active-demands`.
  - `/resource-fulfilment/candidate-details`.
- Added fulfilment workbook upload and refresh via Electron IPC:
  - `fulfilment:getData`.
  - `fulfilment:selectExcel`.
- Selected fulfilment workbook is persisted in Electron `userData/settings.json`.
- Active Demands Dashboard reads the `SF Datadump` worksheet.
- Active Demands Dashboard columns:
  - Status.
  - Offshore/Onsite.
  - Customer.
  - Legacy Job Req Id.
  - Job Requisition ID/Demand ID.
  - Role.
  - Total Positions.
  - Remaining Positions.
  - Aging in Days.
  - # Profiles Received.
  - # TP-1 Selected.
  - # TP-2/Client Selected.
  - # TP-3/Client Selected.
  - # Onboarded.
  - # Renege.
  - PM.
  - CU Mapping.
- Active Demands Dashboard includes multi-select filters for:
  - Customer.
  - Status.
  - PM.
  - Offshore/Onsite.
  - CU Mapping.
- Active Demands summary KPIs are calculated from visible filtered rows:
  - Total Demands.
  - Total Positions.
  - Remaining.
  - Profiles.
  - Onboarded.
- Candidate Details Dashboard is added as a workbook-aware shell pending source
  worksheet and candidate column rules.


v2.5.0 - Fulfilment Candidate Details Dashboard
-----------------------------------------------
- Candidate Details Dashboard now reads `Candidate Tracker`.
- Added Legacy Job Req Id selector from active demands.
- Added parent-child navigation:
  - Double-click Active Demands row.
  - Use Candidates > View action.
  - Back button from Candidate Details to Active Demands.
- Added Candidate Details KPIs:
  - Total Candidates.
  - Screen Select.
  - TP-1 Selected.
  - TP-2 Selected.
  - TP-3 Selected.
  - Offers.
- Added candidate status color coding:
  - Selected.
  - Rejected.
  - Pending.
  - Scheduled.


v2.6.0 - Fulfilment Leadership Review
-------------------------------------
- Added shared fulfilment rule engine:
  - `src/services/fulfilmentAnalysis.js`.
- Added expanded Active Demands KPIs:
  - Fulfilment %.
  - Aging >30.
  - Aging >60.
  - Zero Profiles.
  - Stale Demands.
- Added Demand Funnel view:
  - Remaining.
  - Profiles.
  - Screen Select placeholder.
  - TP-1 Selected.
  - TP-2 Selected.
  - TP-3 Selected.
  - Offers placeholder.
  - Onboarded.
- Expanded Candidate Details KPIs:
  - Screen Select %.
  - TP-1 Select %.
  - TP-2 Select %.
  - TP-3 Select %.
  - Offer Conversion %.
  - Pending Interviews.
  - Rejected.
  - Average Profile-to-Offer days.
- Added Resource Fulfilment route:
  - `/resource-fulfilment/leadership-review`.
- Added Fulfilment Leadership Review page with:
  - Fulfilment Action Queue.
  - Customer Risk Board.
  - PM Accountability Board.
  - Customer Aging Heatmap.
  - PM Aging Heatmap.
  - Role Hotspot View.
  - Weekly Snapshot.
- Fulfilment Action Queue rules include:
  - Critical Aging.
  - No Profiles Received.
  - Low Pipeline.
  - Stale Demand.
  - Onsite Aging Risk.
  - Screening Pending.
  - Selected but No Offer.
  - High Rejection Pattern.
- Added fulfilment snapshot export:
  - `fulfilment:exportSnapshot`.
  - Multi-sheet Excel export with Weekly Snapshot, Action Queue, Customer Risk,
    PM Risk, Role Hotspots, Active Demands, and Candidate Details.


v2.7.0 - Fulfilment Filters and Base Dashboard
----------------------------------------------
- Active Demands parser now captures:
  - Location.
  - Band.
- Active Demands Dashboard now includes multi-select filters for:
  - Location.
  - Band.
- Active Demands grid now includes Location and Band columns.
- Fulfilment workbook parser now reads base sheet:
  - `Fulfillment Details 2025&2026`.
- Added Resource Fulfilment route:
  - `/resource-fulfilment/base-dashboard`.
- Added Fulfilment Base Dashboard with filters:
  - Month.
  - Customer.
  - Location.
  - Hiring Mode.
  - DM.
  - PSA.
- Fulfilment Base Dashboard includes grouped views:
  - Customer Wise.
  - Location Wise.
  - Hiring Mode Wise.
  - DM Wise.
  - PSA Wise.
  - Month Wise.
- Fulfilment Base Dashboard includes detail grid from the base sheet with:
  - Month.
  - Customer.
  - Location.
  - Hiring Mode.
  - DM.
  - PSA.
  - Count.
  - Candidate Name.
  - SAP ID.
  - SR#.
  - Joining/FS Date.
  - Comments.


v2.7.1 - Fulfilment Base Dashboard Layout Refinement
----------------------------------------------------
- Renamed `External Hiring` KPI to `Lateral Hiring`.
- Reordered grouped dashboard cards:
  - Customer Wise Heatmap.
  - DM Wise Heatmap.
  - Hiring Mode Wise.
  - Location Wise.
  - PSA Wise.
  - Month Wise.
- Replaced Customer Wise and DM Wise table cards with compact heatmap block
  cards, sized by fulfilment count.


v2.7.2 - Fulfilment Dashboard Compact Layout
--------------------------------------------
- Active Demands KPI cards are compacted to fit more cards in one row.
- Active Demands filters are compacted, including smaller PM and
  Offshore/Onsite filter widths.
- Active Demands funnel cards are compacted.
- Candidate Details KPI cards are compacted.
- Fulfilment Base Dashboard removes Candidate Rows KPI.
- Fulfilment Base grouped table cards remove the Rows column.
- Fulfilment Base replaces PSA Wise and Month Wise table cards with compact
  heatmap block cards.
- Fulfilment Leadership Review replaces Weekly Snapshot table with compact
  heatmap block cards.


v2.7.3 - Candidate Customer and Final Select Rule Updates
--------------------------------------------------------
- Candidate Details Dashboard now includes Customer from `Candidate Tracker`.
- Candidate Details grid now displays Customer next to Profile Received Date.
- Fulfilment Leadership Review action logic updated for interview flow:
  - Screen Select.
  - TP1 interview.
  - TP2 interview.
  - TP3 interview only for selected customer processes.
- Final select is now treated as:
  - TP2 selected, or
  - TP3 selected where customer process uses TP3.
- Replaced generic `Selected but No Offer` action with
  `Final Select but No Offer`.
- Added `Awaiting Customer Interview Decision` action for candidates who clear
  TP1 but do not yet have TP2/TP3 final selection.
- Current Bottleneck and Recommended Action wording now reflects TP2 as the
  normal final interview decision point, with TP3 called out as customer-specific.


v2.7.4 - Latest Build Status
----------------------------
- Latest Windows installer build completed successfully after v2.7.2 and v2.7.3
  changes.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.7.5 - Active Demands Cascading Filters and Heatmap
-----------------------------------------------------
- Active Demands parser now derives Demand Month from:
  - `Req Date`, with
  - `TRF Requested Date` fallback.
- Active Demands Dashboard now treats Status as the primary filter.
- Customer filter options now cascade from selected Status values.
- Added Month filter to Active Demands Dashboard.
- Replaced ActiveDemandsTable grid with compact heatmap-style demand cards.
- Active Demand heatmap cards exclude filter-only fields:
  - Status.
  - Customer.
  - Month.
  - PM.
  - Offshore/Onsite.
  - Location.
  - Band.
  - CU Mapping.
- Active Demand heatmap cards retain operational fields:
  - Legacy Job Req Id.
  - Role.
  - Total Positions.
  - Remaining.
  - Aging.
  - Profiles.
  - TP1.
  - TP2.
  - TP3.
  - Onboarded.
  - Renege.
  - Candidate Details action.


v2.7.6 - Candidate Stage Heatmap and Billing Type
-------------------------------------------------
- Candidate Details Dashboard now shows Candidate Stage Heatmap above the
  Candidate Pipeline grid.
- Stage heatmap blocks summarize:
  - Profile Received.
  - Screening.
  - TP1 Interview.
  - TP2 Interview.
  - TP3 Interview.
  - Offer.
  - Selected.
  - Rejected.
  - Pending.
  - Scheduled.
  - No Update.
  - Billing Loss.
  - Pro Active.
- Candidate Details grid now includes:
  - Billing Type.
  - Current Stage.
  - Stage Status.
- Billing Type is joined from SF Datadump `Billing Loss`:
  - Yes -> `Billing Loss`.
  - No/blank -> `Pro Active`.
- Stage Status is derived from Candidate Tracker stage columns:
  - Screen Select/Reject.
  - TP1 Status.
  - TP2 Status / Client Interview-1.
  - TP3 Status / Customer Interview-2.
  - Offer Status.
- SharePoint `JCI_JCH_Portfolio.xlsx` was referenced for stage classification,
  but it requires authenticated SharePoint access and was not directly readable
  from this environment.


v2.7.7 - Candidate Stage Classification Workbook
------------------------------------------------
- Candidate Details Dashboard now reads stage classification from local workbook:
  `data/Stage_Classification.xlsx`.
- Source worksheet:
  - `Resourcing`.
- Electron fulfilment data now includes parsed stage groups and stage details:
  - Profile Sourcing.
  - TP1.
  - TP2.
  - Customer Feedback.
  - Onboarding.
- Candidate Stage Heatmap now uses the stage groups from the classification
  workbook instead of only fallback screen/interview labels.
- Candidate Pipeline grid now includes:
  - Stage code.
  - Stage detail from the classification workbook.
- Candidate status flow remains based on Candidate Tracker fields:
  - Screen status maps to Profile Sourcing.
  - TP1 status maps to TP1.
  - TP2 status maps to TP2.
  - TP3 status maps to Customer Feedback.
  - Offer status maps to Onboarding.
- Billing Loss vs Pro Active classification continues to come from SF Datadump
  `Billing Loss`.


v2.7.8 - Fulfilment Billing Type and Stage Card Layout
------------------------------------------------------
- Active Demands Dashboard now shows Billing Loss / Pro Active directly on
  each Active Demand heatmap card.
- Billing type source remains SF Datadump `Billing Loss`:
  - Yes -> Billing Loss.
  - No/blank -> Pro Active.
- Candidate Details Dashboard no longer mixes Billing Loss / Pro Active into
  the stage heatmap.
- Candidate Stage Heatmap is now shown as stage cards:
  - Each card heading shows the stage name and total candidate count.
  - Status blocks are nested under each stage:
    - Selected.
    - Rejected.
    - Pending.
    - Scheduled.
    - No Update.
- Candidate Pipeline keeps stage code, stage detail, current stage, and stage
  status from the stage-classification-driven flow.


v2.7.9 - Demand Identifier Format and Stage Code Blocks
-------------------------------------------------------
- Active Demands heatmap cards now show demand identifiers in the format:
  `Legacy Job Req Id (Job Requisition ID/Demand ID)`.
- Candidate Stage Heatmap stage cards now show detailed stage-code blocks
  under each parent stage using `Stage_Classification.xlsx`.
- Stage-code block ranges are sourced from the `Resourcing` sheet:
  - Profile Sourcing: 1.01 to 1.17.
  - TP1: 2.01 to 2.16.
  - TP2: 3.01 to 3.15.
  - Customer Feedback: 4.01 to 4.10.
  - Onboarding: 5.1 to 5.7.
- Candidate status-to-stage-code matching now uses the candidate stage status
  text and the classification descriptions:
  - Screen status maps to Profile Sourcing stage codes.
  - TP1 status maps to TP1 stage codes.
  - TP2 and TP3 statuses map to TP2 stage codes.
  - Offer status maps to Onboarding stage codes.
- Each stage-code mini-block shows the count for candidates currently matching
  that stage code; the full stage description is available as hover text.


v2.7.10 - Stage Heatmap Parent Card Code Guard
----------------------------------------------
- Candidate Stage Heatmap now enforces stage-code prefixes per parent card:
  - Profile Sourcing only shows `1.*`.
  - TP1 only shows `2.*`.
  - TP2 only shows `3.*`.
  - Customer Feedback only shows `4.*`.
  - Onboarding only shows `5.*`.
- This prevents the Profile Sourcing card from displaying TP1, TP2, Customer
  Feedback, or Onboarding stage codes.


v2.7.11 - Stage Code De-duplication
-----------------------------------
- Candidate Stage Heatmap now de-duplicates stage-code mini-blocks within each
  parent card.
- Profile Sourcing now renders each stage code only once:
  - `1.01` through `1.17`.
- Stage-code blocks are sorted numerically within each parent stage card.


v2.8.0 - Fulfilment Stage Delay Dashboard
-----------------------------------------
- Added new Resource Fulfilment module screen:
  - Stage Delay Dashboard.
- Stage Delay Dashboard uses Candidate Tracker, SF Datadump, and
  Stage_Classification.xlsx to derive:
  - Current candidate stage.
  - Stage code.
  - Stage SPOC.
  - Stage status.
  - Days in current stage.
  - SLA days.
  - Severity: On Track / Watch / Critical.
  - Delay reason.
  - Recommended action.
- The dashboard uses an explicit Candidate Tracker delay field when available:
  - `Stage Delay`
  - `Stage Delay Days`
  - `Delay Days`
  - `Days in Stage`
  - `Current Stage Delay`
  - `Stage Aging`
  - `Stage Ageing`
  - `Aging in Current Stage`
- If the explicit delay field is not filled, days in stage are derived from
  existing stage dates:
  - Profile Received Date.
  - Screen Date.
  - TP-1 Date.
  - TP2 Date / Customer Interview.
  - TP3 Date / Customer Interview.
  - Offer Date, if present.
  - Customer Feedback Date, if present.
  - Onboarding / Joining Date, if present.
- Added TAG Profile Supply Trend:
  - Month-wise profiles received.
  - TAG profiles based on Candidate Tracker `Shared_by`.
- Added data readiness indicators:
  - Missing stage dates.
  - Missing profile owner / `Shared_by`.
  - Delay field filled percentage.
- Candidate Stage Heatmap mini-block colors now represent SPOC ownership from
  the first bracket in stage descriptions:
  - TAG.
  - CU.
  - DU.
- Electron Candidate Tracker parser now captures additional fields:
  - Skill.
  - Candidate status.
  - Shared_by.
  - Talent Navigator Score.
  - Profile shared with Customer TP-2 / TP-3 round.
  - Aging of TP-2 round.
  - Aging of TP-3 round.
  - Offer Date.
  - Customer Feedback Date.
  - Onboarding / Joining Date.
  - Stage delay / days-in-stage aliases.

Data Capture Needs for Stage Delay Accuracy
-------------------------------------------
- Candidate Tracker should consistently update:
  - `Shared_by` for TAG profile supply reporting.
  - Stage status fields for Screen, TP1, TP2, TP3, and Offer.
  - Stage date fields whenever status changes.
  - New stage delay / days-in-stage field, if manually maintained.
- Recommended additional fields:
  - Offer Date.
  - Customer Feedback Date.
  - Onboarding / Joining Date.
  - Action owner.
  - Action due date.
  - Latest follow-up comment.


v2.8.1 - Stage Delay Filter Simplification
------------------------------------------
- Stage Delay Dashboard top KPI cards removed.
- Stage Delay filters simplified to:
  - Demand Status.
  - Customer.
  - PM.
- Stage, SPOC, and Severity filters removed from the filter bar.
- TAG Profile Supply Trend now recalculates from the filtered delay rows, so
  profile trend values respond to selected Demand Status, Customer, and PM.
- Candidate Stage Delay Queue now includes Demand Status as a visible column.


v2.8.2 - Stage Delay Exclusions
-------------------------------
- Stage Delay Dashboard now excludes candidates from delay calculations when
  any candidate stage status indicates:
  - Dropped.
  - Rejected / Screen Reject.
  - No Show.
- Excluded candidates are removed before deriving:
  - Delay by Stage.


v2.8.3 - Stage Delay Cancellation Exclusion Scope
-------------------------------------------------
- Stage Delay Dashboard now also excludes Cancelled candidates from Delay by
  Stage calculations.
- Exclusion statuses now include:
  - Dropped.
  - Rejected / Screen Reject.
  - No Show.
  - Cancelled.
- Exclusion scope is limited to Delay by Stage.
- TAG Profile Supply Trend continues to count profiles after Demand Status,
  Customer, and PM filters are applied, including profiles whose later outcome
  was Dropped / Rejected / No Show / Cancelled.


v2.8.4 - SharePoint / OneDrive Synced Workbook Cache
----------------------------------------------------
- Excel file loading now supports workbooks selected from synced
  SharePoint/OneDrive folders by copying the selected workbook to an Electron
  `userData/workbook-cache` folder before parsing.
- The cache helper is shared across workbook modules:
  - RAS / Resource workbook.
  - PP Analysis workbook.
  - RFS Tracker workbook.
  - Resource Fulfilment workbook.
  - Stage Classification workbook.
- Refresh behavior:
  - Re-reads the original selected workbook path.
  - Copies the latest bytes into the local workbook cache.
  - Parses the cached workbook path with SheetJS.
- Electron settings now persist the selected original workbook path and the
  latest cached workbook path per module.
- SharePoint browser URLs are explicitly rejected with a message telling the
  user to select the synced local file path.
- If a synced file cannot be hydrated/read, the app returns a user-facing
  message asking the user to choose `Always keep on this device` and refresh.
- If the workbook is locked or still syncing, the app returns a user-facing
  message asking the user to close Excel or allow sync to finish and retry.


v2.8.5 - Candidate Details Heatmap Layout Refinement
----------------------------------------------------
- Candidate Details Dashboard KPI cards were removed.
- Candidate Stage Heatmap cards were enlarged so stage/status text is visible.
- Candidate Stage Heatmap status blocks now use a wider two-column layout.
- Stage/status labels wrap instead of being truncated with ellipses.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.8.6 - Active Demand Billing Loss Value
-----------------------------------------
- Active Demands Dashboard now shows the billing loss dollar value next to the
  Billing Loss badge on Active Demand Heatmap cards.
- Billing loss value is read from Column Q in the `SF Datadump` worksheet and
  exposed as `billingLossValue`.
- Billing loss values are formatted as USD currency on Billing Loss cards.
- Pro Active cards continue to show only the Pro Active badge.
- Candidate Stage Heatmap parent cards now use a consistent neutral card color.
- Candidate Stage Heatmap stage-code blocks now show full code and description
  text, for example `1.01-[TAG][EXT] Primary/Secondary skills`.
- Candidate Stage Heatmap hides stage-code mini cards with value 0.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.8.7 - Active Demand Billing Flag Source and Stage Row Layout
---------------------------------------------------------------
- Active Demands Dashboard now uses SF Datadump Column AP as the Billing Loss
  flag for Active Demand Heatmap card badge status.
- SF Datadump Column Q remains the billing loss dollar value source for Billing
  Loss cards.
- Active Demand Heatmap non-billing-loss cards now show `Active` status.
- Candidate Details Dashboard Candidate Stage Heatmap now keeps the five
  parent stage cards in a single row with horizontal overflow when needed.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.8.8 - Fulfilment Heatmap Presentation Refinement
---------------------------------------------------
- Active Demand Heatmap non-billing-loss cards now show `Pro Active` status.
- Candidate Stage Heatmap mini-card counts now use a larger font than the stage
  description text.
- Candidate Stage Heatmap mini-card stage description text now uses gray text
  to visually separate it from the count.
- Fulfilment Base Dashboard Hiring Mode Wise and Location Wise sections now use
  heatmap cards instead of table grids.
- Fulfilment Base Dashboard Hiring Mode Wise and Location Wise heatmaps no
  longer show location/customer count columns.
- Fulfilment Base Dashboard Month Wise and DM Wise heatmaps were swapped in the
  grouped dashboard order.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.8.9 - Generic Stage Classification Workbook Path
---------------------------------------------------
- Stage classification workbook default path is now app/project local:
  `data/Stage_Classification.xlsx`.
- Removed the user-specific Downloads path dependency for
  `Stage_Classification.xlsx`.
- Packaged builds resolve the workbook from Electron `extraResources/data`.
- Development builds resolve the workbook from the project `data` folder.


v2.9.0 - Fulfilment Drilldowns and Data Quality
-----------------------------------------------
- Active Demands Dashboard now includes Billing Loss Summary.
- Billing Loss Summary shows:
  - Billing Loss Demands.
  - Billing Loss Value.
  - Aging >60.
  - Zero Profiles.
  - Top billing loss Customer.
  - Top billing loss PM.
- Billing Loss Summary cards click through into the Active Demand Heatmap by
  applying the matching filter.
- Active Demands Dashboard now includes a Data Quality Panel for:
  - Missing PM.
  - Missing Customer.
  - Missing Legacy Job Req Id.
  - Missing Billing Loss dollar value.
  - Candidate Tracker SR values not found in SF Datadump.
- Candidate Stage Heatmap mini-cards now drill down the Candidate Pipeline by
  selected stage code.
- Candidate Stage Heatmap selected stage drilldown is visually highlighted and
  can be cleared.
- Fulfilment Base Dashboard heatmap cards now apply click-to-filter drilldowns
  for Customer, Month, Hiring Mode, Location, PSA, and DM.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.1 - Active Demand Summary Panel Layout
-------------------------------------------
- Active Demands Dashboard now shows Data Quality Panel above Billing Loss
  Summary.
- Data Quality Panel is collapsible and expanded by default.
- Billing Loss Summary is collapsible and expanded by default.
- Active Demand filters now appear below both summary panels.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.2 - Stage Delay Trend Dashboard
------------------------------------
- Stage Delay Dashboard now includes a Fulfilment Trend Dashboard.
- Trend window selector supports:
  - Last 3 months.
  - Last 6 months.
- Trend summary cards show:
  - Open Demands.
  - Profiles Internal.
  - Profiles External.
  - Onboarding Internal.
  - Onboarding External.
- Month-wise trend chart shows:
  - Open Demands.
  - Profiles Received split by Internal / External.
  - Onboarding split by Internal / External.
- Customer / Month wise trend table shows the same metrics by customer and
  month.
- Trend calculations follow the Stage Delay Dashboard filters:
  - Demand Status.
  - Customer.
  - PM.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.3 - Trend Dashboard Base Onboarding Alignment
--------------------------------------------------
- Resource Fulfilment navigation label changed from Stage Delay Dashboard to
  Trend Dashboard.
- Trend Dashboard page title now shows `Trend Dashboard`.
- Fulfilment Trend Dashboard is now positioned immediately below the Demand
  Status, Customer, and PM filters.
- Trend Dashboard onboarding metrics now read from the
  `Fulfillment Details 2025&2026` sheet so onboarding internal and onboarding
  external/lateral values align with Fulfilment Base Dashboard.
- Onboarding split uses Fulfilment Base Dashboard hiring mode logic:
  - Hiring Mode containing `Internal` -> Onboarding Internal.
  - Hiring Mode containing `External` or `Lateral`, and other non-internal
    modes -> Onboarding External.
- Open demand and profile received trend metrics continue to use SF Datadump and
  Candidate Tracker respectively.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.4 - Trend Dashboard Line Chart and Heatmap
-----------------------------------------------
- Trend Dashboard onboarding labels now align directly to Fulfilment Base
  Dashboard KPI labels:
  - `Internal Hiring` maps to `Onboarding Internal`.
  - `Lateral Hiring` maps to `Onboarding External`.
- Month-wise Trend Dashboard visualization changed from bar chart to line
  chart.
- Customer / Month Wise table was replaced with a Customer / Month Heatmap.
- Heatmap blocks show:
  - Open Demands.
  - Profiles Internal.
  - Profiles External.
  - Onboarding Internal.
  - Onboarding External.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.5 - Trend Dashboard Heatmap Placement and Month Parsing
------------------------------------------------------------
- Trend Dashboard now shows the Customer / Month Heatmap below the month-wise
  line chart as a full-width section.
- Fulfilment base month labels such as `May'26` are now parsed as May 2026
  before generic JavaScript date parsing is attempted.
- This fixes onboarding trend values that previously appeared as zero because
  month labels from `Fulfillment Details 2025&2026` were not matching the trend
  window.
- Onboarding Internal continues to map from `Internal Hiring`.
- Onboarding External continues to map from `Lateral Hiring`.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.6 - Trend Dashboard Fulfilment Percent Placement
-----------------------------------------------------
- Trend Dashboard now includes Internal Fulfilment % and External Fulfilment %
  in KPI cards.
- Fulfilment percentage lines remain available in the month-wise trend chart.
- Customer / Month Heatmap now keeps count-only metric chips:
  - Open.
  - Profiles Internal.
  - Profiles External.
  - Onboarding Internal.
  - Onboarding External.
- Delay by Stage card remains removed from Trend Dashboard.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.7 - Candidate Details StageStatus Source
---------------------------------------------
- SF Datadump parser now captures `StageStatus` from column J.
- Candidate Details Dashboard now derives Candidate Pipeline stage values from
  SF Datadump `StageStatus`:
  - Stage is the first substring before `-`.
  - Stage Detail is the next substring after `-`.
- Candidate Stage Heatmap now reflects the same SF Datadump `StageStatus`
  stage and detail source.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.8 - Candidate Tracker StageStatus Correction
-------------------------------------------------
- Candidate Tracker parser now captures `StageStatus` from column J.
- Candidate Details Dashboard now derives Candidate Pipeline stage values from
  Candidate Tracker `StageStatus`:
  - Stage is the first substring before `-`.
  - Stage Detail is the next substring after `-`.
- Candidate Stage Heatmap now reflects Candidate Tracker `StageStatus` as the
  stage and detail source.
- Candidate Details Dashboard no longer uses SF Datadump `StageStatus` for these
  stage/detail values.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.9 - Candidate Stage Heatmap Parent Order
---------------------------------------------
- Candidate Details Dashboard Candidate Stage Heatmap now always renders the
  five parent cards in this order:
  - Profile Sourcing.
  - TP1.
  - TP2.
  - Customer Feedback.
  - Onboarding.
- Parent stage cards remain visible even when the count is 0.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.10 - Candidate Details Demand ID KPI
-----------------------------------------
- Candidate Details Dashboard now shows a Job Requisition ID/Demand ID KPI card
  next to the Select Legacy Job Req Id selector.
- The KPI value is sourced from the selected SF Datadump demand row's
  `Job Requisition ID/Demand ID` field.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.11 - Active Demand StageStatus Heatmap Counts
--------------------------------------------------
- Active Demand Heatmap now derives the TP1, TP2, Customer, and Onboarded card
  values from Candidate Tracker `StageStatus` instead of SF Datadump summary
  columns.
- Candidate Tracker rows are counted per demand by matching Candidate Tracker
  `SR` to SF Datadump `Legacy Job Req Id`.
- The previous `TP3` heatmap label is now shown as `Customer`.
- TP1 counts Candidate Tracker `StageStatus` codes:
  - `2.01` through `2.10`.
  - `2.13` through `2.16`.
- TP2 counts Candidate Tracker `StageStatus` codes:
  - `3.01` through `3.04`.
  - `3.08` through `3.11`.
- Customer counts Candidate Tracker `StageStatus` codes:
  - `3.12`.
  - `3.14`.
  - `3.15`.
- Onboarded counts Candidate Tracker `StageStatus` codes:
  - `4.01` through `4.10`.
  - `5.1` through `5.7`.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.12 - RFS Year Filter and Detail Export
-------------------------------------------
- RFS Tracker now includes a Year filter.
- RFS detail rows expose `year` from source year fields when present, with a
  selected-quarter sheet suffix fallback.
- Active Source Detail table now includes a Year column.
- RFS export now writes both:
  - Selected-quarter RFS summary.
  - Active Source Detail.
- The export action label is now `Export RFS`.
- Active Source Detail now has its own `Export Detail` button.
- Latest Windows installer build completed successfully after this update.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`


v2.9.13 - Active Demand Billing Loss Drilldown Refinement
---------------------------------------------------------
- Active Demands Dashboard Billing Loss Summary now includes:
  - Billing Loss Demands.
  - Billing Loss Value.
  - Aging >30.
  - Aging >60.
  - Zero Profiles.
  - Top billing loss Customer.
- Removed the Top billing loss PM card from Billing Loss Summary.
- Billing Loss Summary drilldowns now apply specific Active Demand Heatmap
  filters for:
  - Billing loss demand scope.
  - Aging >30 billing loss demands.
  - Aging >60 billing loss demands.
  - Zero profile billing loss demands.
  - Top billing loss customer demand count.
- Clear Billing Drilldown now clears all billing-loss drilldown scopes.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.30 - TAG Dashboard
------------------------
- Resource Fulfilment now includes a `TAG Dashboard`.
- Route:
  - `/resource-fulfilment/tag-dashboard`
- TAG Dashboard reads the TAG demand/supply workbook:
  `C:\Users\nooka.nageswararaok\Downloads\FTE GEO_Demand vs. Supply Report_17-Jun'26 v1.xlsb`.
- Parsed sheets:
  - `Demand data`.
  - `Early Pipeline`.
  - `MTD Offered`.
  - `MTD Renege`.
  - `MTD Declined`.
  - `Yet to Joiner`.
  - `Past Joiner`.
  - `Rejected`.
- Candidate rows are joined to demands by `Legacy Job Req Id`, with
  `Job Requisition ID` as fallback.
- Default filters:
  - LOB: `ERS`.
  - Project L1: `ERS VDU`.
  - Project L4: `ERS VDU-MEGA-MFG-DIGITAL ENGG1`.
- TAG filter section includes:
  - Project L1.
  - Project L4.
  - Demand Status.
  - Customer.
  - Hiring Manager.
  - Application Status.
  - Demand ID.
- Dashboard sections:
  - Executive TAG Snapshot.
  - Early Pipeline Funnel.
  - Application Status Mix.
  - Demand Coverage And Gaps heatmap.
  - Customer Hotspots heatmap.
  - Early Pipeline Candidate Details.
- TAG Dashboard accepts future navigation state with `legacyJobReqId` or
  `demandId` so Active Demand cards can later open TAG details for the selected
  demand without replacing the existing Candidate Details navigation.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.31 - TAG Dashboard Heatmap Refinement
------------------------------------------
- TAG Dashboard removed:
  - Recruiter Productivity.
  - Source Productivity.
- TAG Dashboard converted these sections from grids to heatmap cards:
  - Demand Coverage And Gaps.
  - Customer Hotspots.
- Demand Coverage heatmap cards show demand ID, status, customer, role/skill,
  Total positions from `Number of Openings`, actionable positions, candidate
  count, ageing, and coverage risk.
- Customer Hotspot heatmap cards show customer, coverage %, Total positions
  from `Number of Openings`, actionable positions, candidate count,
  zero-pipeline demand count, ageing >60, rejected, renege, and declined counts.
- The previous internal `Gap` value was calculated as
  `Actionable Position - Early Pipeline candidate count`; it is no longer shown
  on TAG heatmap cards.


v2.9.29 - Quality Profile Dropped / No Show Donut
-------------------------------------------------
- Quality of Profile Trend Dashboard now includes a second donut chart:
  - `Dropped`.
  - `No Show`.
  - `Others`.
- Dropped / No Show classification uses Candidate Tracker `StageStatus` text
  when present and falls back to StageStatus codes:
  - Dropped: `2.12`, `3.07`.
  - No Show: `2.11`, `3.06`.
- `Others` includes every remaining Candidate Tracker status in the selected
  filter scope.
- The Quality of Profile layout now shows the two donut charts as peer panels
  with the summary section below them.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.
- Latest Windows installer build completed successfully after this update:
  `npm.cmd run dist`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`.


v2.9.28 - Active Demand Billing Loss Email Draft
------------------------------------------------
- Active Demands Dashboard Billing Loss Summary cards now include a draft-email
  action, matching the Candidate Details Dashboard email flow.
- Each Billing Loss Summary email uses the same drilldown scope as its card:
  - Billing Loss Demands.
  - Billing Loss Value.
  - Aging >30.
  - Aging >60.
  - Zero Profiles.
  - Top billing loss Customer.
- Drafted email body includes Active Demand Heatmap card details for the
  matching demands:
  - Demand identifiers.
  - Billing type and billing loss value.
  - Role / Skill Cluster.
  - Total, Remaining, Aging, Profiles.
  - TP1, TP2, Customer, Onboarded, and Renege stage counts.
  - Demand Customer and Status.
- Billing Loss Summary email uses Electron `email:draftHtml` and falls back to
  `mailto:` when the desktop email draft bridge is unavailable.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.27 - Quality of Profile Trend Dashboard
--------------------------------------------
- Resource Fulfilment now includes a `Quality of Profile Trend` dashboard.
- Route:
  - `/resource-fulfilment/quality-profile-trend`
- The dashboard reads Candidate Tracker `StageStatus` values and shows a pie
  chart where all values are percentages against total visible profiles.
- Quality buckets:
  - `Profile Sourcing`: stage codes `1.15`, `2.11`, and `2.12`.
  - `TP1 Reject`: stage codes `2.1` and `2.10`.
  - `TP2 Dropped/No show`: stage codes `3.06` and `3.07`.
  - `TP2 Reject`: stage code `3.05`.
  - `Others`: all remaining profiles.
- `2.1` and `2.10` are both counted for TP1 Reject because Excel/source
  formatting can collapse trailing zeroes in stage-code display.
- Filters:
  - Project L4.
  - Demand Status.
  - Customer.
  - Hiring Manager.
  - Profile Month.
- Project L4 defaults to `ERS VDU-MEGA-MFG-DIGITAL ENGG1`, matching the other
  Resource Fulfilment dashboards.
- Latest Windows installer build completed successfully after this update:
  `npm.cmd run dist`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`.


v2.9.25 - Project L4 First Filter Alignment
-------------------------------------------
- Active Demands Dashboard now shows `Project L4` as the first filter.
- Active Demands Dashboard Status, Customer, Month, Hiring Manager,
  Offshore/Onsite, Location, Band, and CU Mapping option lists now derive from
  the selected `Project L4` scope.
- Trend Dashboard now shows `Project L4` as the first filter.
- Trend Dashboard Demand Status, Customer, and Hiring Manager option lists now
  derive from the selected `Project L4` scope.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.14 - Candidate Stage Heatmap Email Draft
---------------------------------------------
- Candidate Details Dashboard Candidate Stage Heatmap now shows a mail action
  beside each populated stage-status mini card.
- The mail action now asks Electron to generate an unsent `.eml` HTML draft and
  open it through the default desktop mail client.
- Draft email subject uses the selected stage-status label.
- Draft email body is now generated as a professional HTML email with:
  - Filter Summary table.
  - Filtered Candidate Details HTML table.
  - Demand identifiers, customer, selected stage status, filtered candidate
    count, and candidate row details.
- The filtered candidate details table in the email body is limited to the
  first seven columns:
  - S.No.
  - Candidate Name.
  - Customer.
  - External/Internal.
  - SAP ID.
  - Stage.
  - Stage Detail.
- If the desktop HTML draft bridge is unavailable, the app falls back to a
  plain-text `mailto:` draft.
- Stage classification continues to be reloaded from
  `data/Stage_Classification.xlsx` on fulfilment refresh, and the corrected
  workbook is included in packaged builds through `extraResources/data`.
- Existing stage-status click-to-filter drilldown remains unchanged.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.
- Electron main/preload syntax checks completed successfully after this update:
  `node --check electron\main.js`
  `node --check electron\preload.js`.


v2.9.15 - Active Demand Renege Stage Count
------------------------------------------
- Active Demand Heatmap `Renege` metric now derives from Candidate Tracker
  `StageStatus` instead of the SF Datadump renege column.
- Renege is counted per demand by matching Candidate Tracker `SR` to SF Datadump
  `Legacy Job Req Id`.
- Renege count uses Candidate Tracker stage code `5.7`, including
  `5.7 [DU] Report renege to L3 Operations team`.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.16 - Active Demand Funnel Stage Count Alignment
----------------------------------------------------
- Active Demands Dashboard Demand Funnel now aggregates stage values using the
  same demand-wise Candidate Tracker `StageStatus` logic as Active Demand
  Heatmap cards.
- Demand Funnel stage cards now summarize visible active demands for:
  - Remaining.
  - Profiles.
  - TP1.
  - TP2.
  - Customer.
  - Onboarded.
  - Renege.
- TP1, TP2, Customer, Onboarded, and Renege counts now come from Candidate
  Tracker rows matched by `SR` to SF Datadump `Legacy Job Req Id`.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.17 - Fulfilment SF Datadump Source Migration
-------------------------------------------------
- Resource Fulfilment dashboards now use `SF Datadump` as the sole base demand
  worksheet going forward.
- `SF Datadump` has two non-data rows before the header row; the parser now
  locates the header row by SF headers such as:
  - `Job Requisition ID`.
  - `Legacy Job Req Id`.
  - `Status`.
- The previous SR/SF mapping worksheet was a one-time migration reference only
  and is no longer read during workbook import.
- Future fulfilment workbooks are not expected to contain the old demand base
  worksheet or the migration mapping worksheet.
- Fulfilment demand fields are now mapped directly from SF headers, including:
  - `Customer Name (AIS)` -> customer.
  - `Hiring Manager` -> PM.
  - `Onshore/Offshore` -> offshore/onsite.
  - `Role Name (Demand Unit)` -> role.
  - `Personnel Sub Area Name` -> location.
  - `Demand Sub Band Name` -> band.
  - `Date Created` -> request date / demand month.
  - `Number of Openings` -> total positions.
  - `Actionable Position` -> remaining positions.
  - `Demand_Ageing_days` -> aging days.
  - `HR L4` -> CU mapping.
  - `Billing Loss flag` -> billing-loss badge status.
  - `Billing Loss` -> billing-loss value.
- Open-demand calculations treat SF `Open` rows as active/open demand rows.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.18 - Active Demand Project L4 Filter
-----------------------------------------
- Active Demands Dashboard now parses `Project L4` from `SF Datadump`.
- Added a `Project L4` multi-select filter to the Active Demands Dashboard.
- Active Demand filters now have a compact filter-section header with a top
  `Clear Filters` action, matching the summary-panel action placement and
  freeing the filter grid for filter controls.


v2.9.19 - Active Demand Filter Width and Status Alignment
---------------------------------------------------------
- Active Demands Dashboard filter label changed from `PM` to `Hiring Manager`
  to match the `SF Datadump` source header.
- Active Demands Dashboard filter layout now gives more width to `Project L4`
  and `CU Mapping` by reducing the Month, Location, and Band filter widths.
- Active/open demand calculations now use only SF status `Open`.
- `On Hold` and `Closed` remain available in the Status filter but are not
  counted as active/open demand rows in active-demand KPIs or funnel counts.


v2.9.20 - Active Demand Card Footer and Initial Render Performance
------------------------------------------------------------------
- Active Demands Dashboard now defaults the `Project L4` filter to
  `ERS VDU-MEGA-MFG-DIGITAL ENGG1` on initial load.
- `Clear Filters` still clears all filters, including the default Project L4
  value.
- Active Demand Heatmap cards now show Customer at the bottom-left of each
  card.
- Demand rows with Status `On Hold` now show an amber `ON HOLD` badge at the
  bottom-right of each card.
- Active Demand Heatmap rendering is capped to the first 600 currently visible
  demand cards to avoid mounting thousands of cards after a broad filter clear;
  the header still shows the full visible demand count.


v2.9.21 - Active Demand KPI and Funnel SF Scope Correction
----------------------------------------------------------
- Active Demands Dashboard KPI cards now aggregate the currently filtered
  `SF Datadump` demand rows directly instead of applying an additional
  open-only status filter.
- The first KPI card is now labeled `Filtered Demands` because it reflects the
  current filter scope, including Status filter selections such as `Open`,
  `On Hold`, or `Closed`.
- Demand Funnel cards now aggregate from the mapped `SF Datadump` columns:
  - `Actionable Position` -> Remaining.
  - `Profiles Received` -> Profiles.
  - `TP-1 Selected` -> TP1.
  - `TP-2/Client Selected` -> TP2.
  - `TP-3/Client Selected` -> Customer.
  - `Onboarded` -> Onboarded.
  - `Renege` -> Renege.
- Candidate Tracker stage-status counts remain available for heatmap card
  stage chips, but the summary KPI and Demand Funnel values now follow the
  selected filter criteria and SF Datadump field mapping.


v2.9.22 - Trend and Fulfilment Base SF Filter Alignment
-------------------------------------------------------
- Trend Dashboard now includes a `Project L4` filter sourced from
  `SF Datadump`.
- Trend Dashboard defaults `Project L4` to
  `ERS VDU-MEGA-MFG-DIGITAL ENGG1`.
- Trend Dashboard filter label changed from `PM` to `Hiring Manager`.
- Trend Dashboard `Clear Filters` action moved to the top filter-section
  header.
- Trend Dashboard filtering now applies Project L4 consistently to:
  - Candidate Stage Delay Queue.
  - Fulfilment Trend Dashboard demand rows.
  - Candidate profile trend rows.
  - Onboarding rows joined from `Fulfillment Details 2025&2026`.
- Fulfilment Base Dashboard now enriches `Fulfillment Details 2025&2026` rows
  from `SF Datadump` by matching `SR#` to `Legacy Job Req Id` or
  `Job Requisition ID` to `Job Requisition ID`.
- Fulfilment Base Dashboard now defaults `Project L4` to
  `ERS VDU-MEGA-MFG-DIGITAL ENGG1`.
- Fulfilment Base Dashboard now includes `Project L4` and `Hiring Manager`
  filters and detail-grid columns.
- Fulfilment Base row count now defaults to 1 per fulfilment detail row when
  the workbook does not contain a `Count` column.


v2.9.23 - Trend Open Demand SF Balance Alignment
------------------------------------------------
- Trend Dashboard `Open Demands` now sums `Balance Positions` from
  `SF Datadump` for rows where Status is `Open`.
- This replaces the previous trend behavior that counted unique demand rows,
  which under-counted open demand volume when a demand had multiple balance
  positions.
- Month-wise trend lines, trend summary cards, and Customer / Month Heatmap
  `Open` chips now all use the same SF `Balance Positions` aggregation.


v2.9.24 - Trend Dashboard Excel Formula Demand Metrics
------------------------------------------------------
- Trend Dashboard now includes the Excel-aligned demand metrics:
  - New Demands: count of `SF Datadump` rows where `Date Created` falls inside
    the month.
  - New Positions: sum of `Number of Openings` for demands created in the
    month.
  - Open Demands: month-end backlog where `Date Created` is before the next
    month, `Closed_Date` is blank or on/after the next month, and Status is not
    `On Hold`.
  - Open Positions: sum of `Number of Openings` for the same month-end backlog
    demand scope.
- Existing profile received, onboarding, and fulfilment percentage trend
  metrics remain in the Trend Dashboard.
- `SF Datadump` parsing now carries `Closed_Date` and `Number of Openings` for
  Trend Dashboard calculations.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.


v2.9.26 - Project L4 First Filter Alignment and Trend Validation
----------------------------------------------------------------
- Active Demands Dashboard shows `Project L4` as the first filter.
- Active Demands Dashboard Status, Customer, Month, Hiring Manager,
  Offshore/Onsite, Location, Band, and CU Mapping option lists derive from the
  selected `Project L4` scope.
- Trend Dashboard shows `Project L4` as the first filter.
- Trend Dashboard Demand Status, Customer, and Hiring Manager option lists
  derive from the selected `Project L4` scope.
- Trend Dashboard `Project L4` options now source directly from `SF Datadump`
  demand rows instead of candidate delay rows, so valid Project L4 values are
  no longer collapsed to the default value plus `Unassigned`.
- Excel serial `Closed_Date` values with fractional time are parsed as local
  date-only values before month-end comparisons.
- Trend Dashboard validation for `ERS VDU-MEGA-MFG-DIGITAL ENGG1` matches
  `data/Trend.png`:
  - Jan-2026: New Demands 24, New Positions 42, Open Demands 55,
    Open Positions 97.
  - Feb-2026: New Demands 39, New Positions 42, Open Demands 61,
    Open Positions 94.
  - Mar-2026: New Demands 23, New Positions 25, Open Demands 29,
    Open Positions 46.
  - Apr-2026: New Demands 27, New Positions 50, Open Demands 29,
    Open Positions 63.
  - May-2026: New Demands 51, New Positions 88, Open Demands 35,
    Open Positions 76.
  - Jun-2026: New Demands 17, New Positions 21, Open Demands 45,
    Open Positions 83.
- Current-month trend calculations use current date as the as-of cutoff.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.
- Latest Windows installer build completed successfully after this update:
  `npm.cmd run dist`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.0-Setup.exe`.


v2.9.32 - PP Oversized XLSX Base Streaming
------------------------------------------
- Fixed PP Report Analysis upload for the Jun FY26 `.xlsx` workbook:
  `ERS MEGA - GM Trend YTD Jun'26_MFG.xlsx`.
- Root cause:
  - The workbook contains a `Base` worksheet, but `xl/worksheets/sheet4.xml`
    expands to about 1.16 GB.
  - SheetJS cannot materialize that worksheet as one string and previously
    returned no loaded `Base` worksheet, which surfaced as:
    `No worksheets found in pp_..._ERS_MEGA_-_GM_Trend_YTD_Jun'26_MFG.xlsx`.
- PP parsing now falls back to a targeted streaming XLSX parser for oversized
  `.xlsx` `Base` sheets.
- Validation:
  - The Jun workbook copy parsed `Base` successfully with 302,159 data rows.
  - Electron main syntax check completed successfully:
    `node --check electron\main.js`.
  - Production renderer build completed successfully:
    `npm.cmd run build`.


v2.9.33 - PP Project Expense Source Column Alignment
----------------------------------------------------
- Cross-checked PP Report Analysis against:
  `ERS MEGA - GM Trend YTD Jun'26_MFG.xlsx`.
- Source validation:
  - `Base` sheet contains 302,159 data rows.
  - `Total Project Expenses` exists and totals 429,633.36 across the full
    workbook.
  - In the default `DIGITAL ENGG1` L4 scope, `Total Project Expenses` is zero
    for `Jun'26`, while `Project Expenses` is 9,034.26 for `Jun'26`.
- PP parsing now maps the `Total Project Exp` KPI from source column
  `Project Expenses` when present, with `Total Project Expenses` retained as
  fallback for older workbooks.
- Validation:
  - Electron main syntax check completed successfully:
    `node --check electron\main.js`.
  - Production renderer build completed successfully:
    `npm.cmd run build`.

v2.9.34 - Active Demand Referral Email Draft
--------------------------------------------
- Active Demands Dashboard now includes an `Email Demands` action in the
  Active Demand Heatmap header.
- The email drafts the complete currently filtered demand scope, including
  rows beyond the heatmap's first-600-card render cap.
- Email body addresses the team with the referral request text and includes
  only these demand detail columns:
  - Demand.
  - Role / Skill Cluster.
  - Band.
  - Remaining.
  - Location.
  - Status.
- Band is sourced from `Demand Sub Band Name` in `SF Datadump`.
- The action uses Electron `email:draftHtml` and falls back to a plain-text
  `mailto:` draft when the desktop email draft bridge is unavailable.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.

v2.9.35 - PP Total Project Expense Source Reversion
---------------------------------------------------
- PP Report Analysis `Total Project Exp` KPI now maps from source column
  `Total Project Expenses`.
- `Project Expenses` is retained only as a fallback when older workbooks do not
  contain `Total Project Expenses`.
- Honeywell validation against
  `ERS MEGA - GM Trend YTD Jun'26_MFG.xlsx`:
  - All Honeywell rows: `Total Project Expenses` = 3,280.38, rounded to 3,280.
  - Honeywell `Jun'26` + `DIGITAL ENGG1`: `Total Project Expenses` = 0.
- Validation:
  - Electron main syntax check completed successfully:
    `node --check electron\main.js`.
  - Production renderer build completed successfully:
    `npm.cmd run build`.



v2.9.36 - PP Bad Depts KPI
--------------------------
- PP Report Analysis now includes `Bad Depts` in the detailed customer KPI
  comparison and Grand Total Summary.
- `Bad Depts` is summed like the other direct PP KPI measures across selected
  customers, current period, comparison periods, and exports.
- PP parsing accepts `Bad Depts` / `Bad depts` and `Bad Debts` / `Bad Debt`
  header spelling variants.
- Validation:
  - Electron main syntax check completed successfully:
    `node --check electron\main.js`.
  - Production renderer build completed successfully:
    `npm.cmd run build`.


v2.9.37 - Active Demand Onsite Offshore Badge
---------------------------------------------
- Active Demand Heatmap cards now show the `Onshore/Offshore` value next to
  the `Pro Active` / `Billing Loss` badge.
- The value uses the existing parsed `offshoreOnsite` field from `SF Datadump`.
- Production renderer build completed successfully after this update:
  `npm.cmd run build`.




v2.9.38 - Candidate Stage Date Badge And Nav Collapse
-----------------------------------------------------
- Candidate Details Dashboard Candidate Stage Heatmap stage-status cards now
  show a `Last Profile` badge with the latest `Profile Received Date` for that
  stage-code group.
- The left navigation panel now auto-collapses after a route is selected from
  the navigation menu.
- Validation:
  - Production renderer build completed successfully:
    `npm.cmd run build`.


v2.9.39 - App Version Display
-----------------------------
- The main left navigation now shows the current app version from
  `package.json`.
- The expanded navigation shows `Version x.y.z`; collapsed navigation shows
  the compact `vx.y.z` marker.



v2.9.40 - Searchable Multi-Select Filters
-----------------------------------------
- Active Demands Dashboard filters now use searchable multi-select dropdowns
  instead of native multi-select list boxes.
- RAS top-bar filters for Customer, Project, PM Name, Location, and HR L4 now
  use searchable multi-select dropdowns.
- RAS Master filters now use searchable multi-select dropdowns for Customer,
  Project, PM Name, Billing Status, WBS Code Type, Band, Location, and HR L4.
- RAS filtering now applies HR L4 through the normalized `capability` field.
- Validation:
  - Production renderer build completed successfully:
    `npm.cmd run build`.


v2.9.41 - UI Version 1.0.1 Build
--------------------------------
- Updated the UI/package version from `1.0.0` to `1.0.1`.
- The left navigation version display now shows `Version 1.0.1`.
- Latest Windows installer build completed successfully after this update:
  `npm.cmd run dist`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.1-Setup.exe`.


v2.9.42 - Customer Profile Performance Dashboard
------------------------------------------------
- Added Resource Fulfilment route:
  - `/resource-fulfilment/customer-profile-performance`.
- Added `Customer Profile Performance` dashboard for customer-wise post-TP1
  profile movement across active demands.
- Dashboard reads:
  - Demand rows from `SF Datadump`.
  - Candidate rows from `Candidate Tracker`.
  - Demand/candidate join via demand `legacyJobReqId` and candidate `sr`.
- Dashboard filters:
  - Project L4.
  - Customer.
  - Demand Status.
  - PM.
  - Location.
- Default filters:
  - Project L4 = `ERS VDU-MEGA-MFG-DIGITAL ENGG1`.
  - Demand Status = `Open`.
- KPI strip includes:
  - Demands.
  - Total Positions.
  - Open Positions.
  - Submitted.
  - Selected.
  - Rejected.
  - Pending Feedback.
  - Dropped.
  - No Show.
  - Selection %.
- `Submitted` is derived as:
  - Rejected + Selected + Pending Feedback + Dropped + No Show.
- Stage status buckets are based on Candidate Tracker `StageStatus` code:
  - Rejected: `3.05`, `3.13`.
  - Selected: `3.11`, `3.12`, `4.01`, `4.02`, `4.03`, `4.05`,
    `4.07`, `4.08`, `4.09`, `5.1`, `5.2`, `5.3`, `5.4`, `5.5`,
    `5.6`.
  - Pending Feedback: `3.08`, `3.14`.
  - Dropped: `3.07`.
  - No Show: `3.06`.
- Customer Demand Profile Performance table includes:
  - S No.
  - DemandId.
  - Role + Skill Cluster.
  - Total Positions.
  - Open Positions.
  - Submitted.
  - Rejected.
  - Selected.
  - Pending Feedback.
  - Dropped.
  - No Show.
  - Selection %.
  - Profiles action.
- Removed the `Feedback Pending %` column and KPI.
- Added a Total row at the bottom of the Customer Demand Profile Performance
  table.
- `Profiles` action opens Candidate Details Dashboard for the selected demand.
- KPI cards for submitted/selected/rejected/pending/dropped/no-show apply
  demand-level drilldowns.
- Project L4 is the first filter in the dashboard.
- Updated the UI/package version from `1.0.1` to `1.0.2`.
- Latest Windows installer build completed successfully:
  `npm.cmd run dist`.
- Latest installer artifact:
  `release/Resource Assignation System (RAS)-1.0.2-Setup.exe`.


v2.9.43 - Fulfilment Data Readiness Score
-----------------------------------------
- Active Demands Dashboard Data Quality Panel now presents a weighted Data
  Readiness Score instead of only raw missing-value cards.
- The score is based on:
  - SF required field completeness for Hiring Manager, Customer, and Billing
    Loss value where Billing Loss is Yes.
  - Candidate Tracker stage date chain completeness.
  - Closed_Date completeness for SF Datadump rows where Status is Closed.
  - Candidate Tracker SR match rate against SF Datadump demand identifiers.
  - Demand Master coverage for SF Datadump rows where Status is Open or On
    Hold.
- Removed the previous Missing Demand ID and Unmatched Candidates KPI cards.
- The Demand Master coverage card shows the missing open/hold demand count
  first, with coverage percentage in the caption.
- Fulfilment workbook parsing now reads the existing `Demand Master` sheet and
  returns normalized demand identifiers to the renderer.


v2.9.44 - Active Demand Filtered Data Quality Build
---------------------------------------------------
- Active Demands Dashboard Data Quality Panel now recalculates from the
  currently filtered demand scope.
- Candidate Tracker readiness checks are scoped to candidate rows linked to
  the filtered SF Datadump demands.
- Updated the UI/package version from `1.0.2` to `1.0.3`.


v2.9.45 - RAS Skill Cluster Internal Match Build
------------------------------------------------
- RAS resource parsing now reads `Skill Cluster` as a separate normalized
  resource field from the resource input workbook.
- Active Demands internal match suggestions now compare demand role and skill
  cluster against RAS resource skills, HR L4 capability, and Skill Cluster.
- Resource detail drilldowns now display Skill Cluster.
- RAS Skill & Capability view now includes Skill Cluster filtering, summary,
  and heatmap coverage.
- Updated the UI/package version from `1.0.3` to `1.0.4`.
